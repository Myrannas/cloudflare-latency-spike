{
  "private": true,
  "name": "perf-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "start": "miniflare ./index.mjs --watch",
    "deploy": "yarn wrangler publish"
  },
  "author": "Michael Oates <moates@atlassian.com>",
  "devDependencies": {
    "@cloudflare/wrangler": "^1.19.8",
    "prettier": "^1.18.2"
  }
}
